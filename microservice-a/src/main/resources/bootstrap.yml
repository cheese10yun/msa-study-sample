spring:

  profiles:
    active: default

  cloud:
    config:
      uri: http://localhost:8888
  application:
    name: microservice-a



management:
  endpoints:
    web:
      exposure:
        include: "*"